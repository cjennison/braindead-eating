# Brain Dead Eating

## iOS Development

After pulling changes, sync the Capacitor config to the iOS project:

```bash
CAPACITOR_SERVER_URL=https://braindead-eating.vercel.app npx cap sync ios
```

Then open in Xcode:

```bash
npx cap open ios
```
